import 'package:flutter/material.dart';

class VisitMapPage extends StatelessWidget {
  const VisitMapPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Visit Map'),
    );
  }
}
